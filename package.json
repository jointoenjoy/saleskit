{
  "name": "chill-saleskit",
  "version": "1.0.0",
  "private": true,
  "description": "練息場 Chill Saleskit — 五感策展級企業體驗簡報（含 /chill/mng 後台）",
  "scripts": {
    "build": "./build.sh",
    "dev": "./build.sh && wrangler pages dev dist --port 8788",
    "deploy": "./build.sh && wrangler pages deploy dist --project-name=saleskit"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
